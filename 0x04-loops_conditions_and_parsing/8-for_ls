#!/usr/bin/env bash
# This script lists current directory contents, showing only the part after the first dash in each filename

for file in *; do
  # Skip if it's not a regular file
  if [ -f "$file" ]; then
    # Extract the part after the first dash, or whole name if no dash
    echo "${file#*-}"
  fi
done
